
# 선형대수

##

## 용어
- 내적(Inner Product)
  - 두 벡터의 성분별 곱의 합, 유사도(거리) 계산의 핵심 
$$
⟨u, v⟩ = Σᵢ uᵢvᵢ = u₁v₁ + u₂v₂ + ⋯ + uₙvₙ
$$
  
    
- 확률벡터(Random Vector)
  
$$
X = (X₁, X₂, …, Xₙ)ᵀ \\

E[X] = (E[X₁], …, E[Xₙ])ᵀ \\

Cov(X) = E[(X−μ)(X−μ)ᵀ] \\
$$
