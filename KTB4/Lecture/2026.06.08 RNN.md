### 사전 훈련 모델(Pre-trained Model)
pre-train 모델은 데이터와 시간이 부족할때 사용한다. 성능 평가할때 정확도와 손실을 비교해서 사용해야만한다.
그 모델에는 깊은 신경망 구조를 사용한 VGC16 ,입력값을 출력값에 직접 더하는 레지듀얼 커넥션을 이용해서 기울기소실 발생 확률을 줄인 ResNet이 있습니다.

### 전이 학습(Transfer Learning)
- 학습시간 단축 및 성능을 개선을 위해서 사전 훈련된 모델에서 피처 또는 가중치를 추출하는 학습기법입니다.
  - 파인튜닝(Fin tuning): 기존 가중치를 기반으로 새로운 데이터셋으로 재학습 하는 방식
    -  사용 이유: 학습시간 단축 및 데이터 부족 문제 극복  
  - 특징추출(Feature Extraction): 합성곱 계층을 유지함으로서 (가중치,특징맵)을 그대로 사용한채로  분류기만 학습하는 방식
    -  적은 연산량과 적은 데이터 셋으로 학습가능
  - 제로샷 학습: 훈련되지 않은 정보를 학습된 내용과 연관지어 인지 및 추론하여 예측시키는 학습기벙입니다. 미리 레이블을 세팅한다.
 
### 오버피팅
  훈련데이터에 너무 학습되서 새로운 데이터에 대한 예측이 안좋음
  - 방지법 drop out
	  - 데이터증강
	  - 정규화(Regularization)
      - 손실에대 패널티항(L1,L2)을 추가하는 방식 
	  - 교차검증
	  - 조기종료
	  - 앙상블

 - 반대개념인 언더피팅이 있습니다.

### Hyperparameter Tuning
- 학습속도,배치크기,에포크,옵티마이저,모멘텀,드롭아웃비율,......
	- 활성화함수: 독자적이기때문에 초매개변수취급함
	- 초기화 : 랜덤학습이 빨리됨
* 레귤레이션 파라미터:
	패널티 텀: 가중치를


### N-gram Language Model
  학습한 단어만 내뱉고 새로운단어는 내뱉지 못하는 언어희소성문제를 가짐.
  원핫인코딩에서 단어의 수만큼 차원이 증가하는 차원의 저주문제 발생
  ->원핫인코딩을 해결하기위해 워드 임베딩(실수벡터)을 사용해서 의미적 유사성과 맥락의 유사성을 반영하는 표현방식

# 인공지능 NLP

## 전통적인 언어 모델

### 언어모델(Language Model)
- 다음 단어가 나타날 확률을 기반으로 계산하는 모델
- 예시) SLM: 통계적 언어모델

### N-gram Language Model

### NPLM

### RNN
