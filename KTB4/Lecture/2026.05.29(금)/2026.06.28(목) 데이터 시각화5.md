# SciPy & 통계적 시각화
---

## 1. SciPy 
- 과학 계산과 통계 분석을 위한 고급 수학 함수와 알고리즘을 제공하는 라이브러리
- 제공 알고리즘 예시: 푸리에 변환, 웨이브렛 변환, 고유값 분해, LU 분해
- 사용 이유: Numpy기반 연산과 보다 좋은 고급 기능 제공 
- 모듈
  ~~~
  import numpy as np

  import scipy

  form scipy import optimize ## 최적화 모듈
  from scipy import stats ## 통계 모듈
  from scipy import signal ## 신호 처리 모듈
  form scipy import interpolate ## 보간법 모듈
  form scipy import intergrate ## 적분 및 미분 방정식 모듈
  from scipy import linalg ## 선형대수 모듈
  ~~~

----

## 2. 정규 분포(Normal Distribution)
- 데이터가 평균을 중심으로 좌우 대칭인 종모향 곡선 확률분포
- 확률분포: 어떤 확률 변수가 취할 수 있는 모든 값과 그 값의 발생확률을 나타내는 함수
    예) 주사위 1/6 확률
- 정규 분포의 확률 밀도 함수(PDF: Probability Density Function)
  -  연속형 확률 분포에서 특정 구간에 값이 나타날 가능성을 설명하는 함수
  -  PDF 값이 큼 -> 해당 구간에 데이터가 많이 분포할 확률이 높음
  -  전체 면적은 항상 1
  -  확률(%): 특정구간의 아레 면적을 적분한 값이 해당 구역의 확률
      - 특정 지점이 확률(%)을 의미하는 것은 아님!
     - $`a <= x <= b $`$ 의 확률 = $`\int_{a}^{b} f(x) dx`$
        
  - 수학적 표현식
     
       $$f(x) = (1 / (σ * sqrt(2 * π))) * exp(- (x - μ)² / (2 * σ²))$$      
  
  | 구분 |	설명 |
  |-----|-----|
  | μ(mu) = 평균(Mean) | 데이터의 중심 위치를 결정(0이면 분포의 중심이 0) |
  | σ(sigma) = 표준 편차(Standard Deviation) | 데이터가 퍼져 있는 정도를 결정 |
  | π(pi) |	약 3.14159 |
  | exp() | 자연로그의 밑 e를 지수로 하는 함수 |
- 시각화 예제 코드
  ~~~
  import numpy as np  
  import scipy.stats as stats  # SciPy의 통계 모듈
  import matplotlib.pyplot as plt  
  import seaborn as sns  

  ## 난수를 이용한 정규분포 데이터 생성
  # 난수 생성 시드 값 설정
  np.random.seed(42)  
  # ( 평균(loc)= 50, 표준 편차(scale)= 10, 정규 분포 난수의 수(size) = 1000 )를 생성
  data = np.random.normal(loc=50, scale=10, size=1000) 
  # 생성된 데이터 배열의 상위 5개 값을 출력
  print(data[:5])

  ## 그래프 생성  
  # 그래프 크기 가로 8인치, 세로 5인치 설정
  plt.figure(figsize = (8,5))
  sns.histplot(data, bins=30, kde=True, color="skyblue", alpha=0.7)
  plt.title("Normal Distribution")
  plt.xlabel("Value")
  plt.ylabel("Frequency")
  plt.show()

  ## 출력: [54.96714153 48.61735699 56.47688538 65.23029856 47.65846625]
  ~~~
  <img width="695" height="470" alt="image" src="https://github.com/user-attachments/assets/d039146e-5702-445b-94b8-7df8eb50df6a" />


## 3. 기술 통계(Descriptive Statistics)

## 4. 가설 검정(Hypothesis Testing)

## 5. 통계적 시각화(Statistical Visualization)
